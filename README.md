# undefined

Created with InitKit CLI

## Setup

1. Install dependencies:
```bash
npm install
```

2. Run the development server:
```bash
npm run dev
```

3. Open [http://localhost:3000](http://localhost:3000)

## Tech Stack

- **Next.js 15** - React framework
- **React 19** - UI library
- **TypeScript** - Type safety
- **Tailwind CSS v4** - Styling

## Folder Structure

```
src/
├── features/       # Feature modules
│   ├── auth/       # Authentication
│   ├── dashboard/  # Dashboard
│   └── users/      # Users
├── shared/         # Shared code
├── app/            # Next.js App Router
├── lib/            # Utilities
└── public/         # Static files
```

## Next Steps

1. Run `npx create-next-app@latest . --use-npm` to initialize Next.js
2. Install Tailwind v4: `npm install tailwindcss@next`
3. Start building in `src/features/`
4. Add environment variables in `.env.local`

---

Built with InitKit
